# Python
__pycache__/
*.pyc
.venv/
.pytest_cache/
.ruff_cache/
*.egg-info/

# Claude Code local state (e.g. isolated agent worktrees) — never repo content
.claude/

# Data cache managed by tools/datasets (see mx-data) — never commit fetched/generated data
.data/

# Secrets — never commit these; see .github/SETUP.md's Secrets and variables section
.env
.env.*
*.pem
*.key
