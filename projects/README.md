# projects/

One subtree per experiment (e.g. `projects/em-piml/`). Each project is
its own `uv` workspace member (own `pyproject.toml`) with its own scoped
`CLAUDE.md` — see the root `CLAUDE.md`'s "Scaling principles" section.
