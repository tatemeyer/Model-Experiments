# Change Orders

Empty until `../README.md` reaches Rev-0. Once it does, any further change
to that document is a new file here (not an in-place edit) — see
`docs/design/README.md`'s "Change Orders" section for the process.
